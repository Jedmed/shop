<nav class="row navbar navbar-expand-md navbar-light bg-light">
  <div class="container">
    <% if (!currentAdmin) { %>
    <a href="/shop/cart" class="navbar-brand fa fa-shopping-cart"><span class="badge"><% if (currentCart) { %>
<%= currentCart.totalQty %>
<% }  %></span></a>
    <% } %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

    <div class="collapse justify-content-end navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav">
        <% if (!currentUser && !currentAdmin) { %>
          <li class="nav-item"><a href="/sessions/new" class="nav-link">Log In</a></li>
          <li class="nav-item"><a href="/users/new" class="nav-link">Register</a></li>
          <% } %>
            <% if(currentUser) { %>
              <li class="nav-item"><a href="/sessions/index" class="nav-link">My Account</a></li>
              <form class="form-inline" action="/sessions?_method=DELETE" method="POST">
                <input type="submit" class="logout" value="Logout" />
              </form>
              <% } %>
                <% if(currentAdmin) { %>
                  <li class="nav-item"><a href="/admin/shop" class="nav-link">Admin Panel</a></li>
                  <form class="form-inline" action="/admin?_method=DELETE" method="POST">
                    <input type="submit" class="logout" value="Logout" />
                  </form>
                  <% } %>
      </ul>
    </div>
  </div>
</nav>
